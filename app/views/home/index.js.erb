$('#my-posts').append('<%= j render @packages %>');
<% if @posts.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @packages %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>